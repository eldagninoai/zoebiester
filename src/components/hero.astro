---
import { Picture } from "astro:assets";
import heroImage from "@/assets/ingrid-hero.png";
import Link from "@/components/ui/link.astro";
import { Icon } from "astro-icon/components";
---

<main
  class="grid lg:grid-cols-2 place-items-center pt-16 pb-8 md:pt-12 md:pb-24">
  <div class="py-6 md:order-1 hidden md:block">
    <Picture
      src={heroImage}
      alt="Ingrid Carranza Biester, psicologa y psicoterapeuta"
      widths={[200, 400, 600]}
      sizes="(max-width: 800px) 100vw, 620px"
      loading="eager"
      format="avif"
    />
  </div>
  <div>
<div class="inline-block mb-4 px-4 py-2 bg-teal-100 text-teal-700 rounded-full font-semibold text-sm animate-pulse-soft shadow-md">
  🎁 Primera sesión completamente gratuita
</div>
  <h1 class="text-4xl lg:text-5xl font-bold lg:tracking-tight text-slate-800 leading-tight">
    Logoterapeuta y Consejera Especialista en Adicciones en CDMX
  </h1>
  <p class="text-base lg:text-lg mt-4 text-slate-600 max-w-xl leading-relaxed">
    Acompañamiento profesional para superar adicciones y encontrar sentido de vida. 
    <span class="block mt-1">Consultas presenciales en Col del Valle, CDMX y en línea.</span>
  </p>
  <div class="mt-6 flex flex-col sm:flex-row gap-3">
    <Link href="/contact" size="lg" class="flex gap-1 items-center justify-center" rel="noopener">
      <Icon class="text-white w-5 h-5" name="bx:bxs-phone-call" />
      Agenda tu sesión gratuita
    </Link>
  </div>
</div>
  </main>
